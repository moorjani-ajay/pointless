coverage:
  status:
    project:
      default:
        target: auto
        threshold: 2% # allow small dips without failing the PR
    patch:
      default:
        target: 70% # new/changed lines should be reasonably covered
comment:
  layout: 'reach, diff, files'
  require_changes: true
